# Sunday 22:00 UTC = Monday 03:30 IST — after Sunday night's backup.

[Unit]
Description=JSK CRM weekly restore verification timer

[Timer]
OnCalendar=Sun *-*-* 22:00:00
Persistent=true
Unit=jsk-crm-backup-verify.service

[Install]
WantedBy=timers.target
